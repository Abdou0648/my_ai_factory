name: Build My App
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Preparing Factory
        run: echo "The AI is building..."
      - name: Save Result
        uses: actions/upload-artifact@v4
        with:
          name: my-final-app
          path: README.md
